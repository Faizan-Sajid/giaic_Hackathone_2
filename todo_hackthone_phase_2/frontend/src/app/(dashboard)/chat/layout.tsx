import { ReactNode } from 'react';

/**
 * Layout for the Chat page
 *
 * Task: TASK-013
 * Spec: Provides consistent layout for the chat UI
 */
export default function ChatLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}